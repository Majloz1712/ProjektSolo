# --- PostgreSQL ---
PGUSER=postgres
PGPASSWORD=CHANGE_ME
PGHOST=127.0.0.1
PGDATABASE=postgres
PGPORT=5432

# --- MongoDB ---
# Uwaga: kod akceptuje MONGO_URI, część narzędzi używa też MONGO_URL.
MONGO_URI=mongodb://127.0.0.1:27017
MONGO_URL=mongodb://127.0.0.1:27017
MONGO_DB=inzynierka

# --- API / auth ---
PORT=3001
JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET
JWT_EXPIRES=7d

# --- SMTP (worker powiadomień) ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=example_user
SMTP_PASS=CHANGE_ME
SMTP_FROM="ProjektSolo <no-reply@example.com>"

# --- LLM / Ollama ---
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_TEXT_MODEL=qwen2.5:7b
