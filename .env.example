# --- Serwer ---
PORT=3001
APP_BASE_URL=http://localhost:3001
JWT_SECRET=change-me-in-local-env
JWT_EXPIRES=1h

# --- PostgreSQL ---
PGUSER=postgres
PGPASSWORD=devpass
PGHOST=127.0.0.1
PGDATABASE=postgres
PGPORT=5432

# --- MongoDB ---
MONGO_URI=mongodb://127.0.0.1:27017
MONGO_DB=inzynierka

# --- SMTP (opcjonalne; wymagane dla resetu hasła/powiadomień e-mail) ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=change-me
SMTP_FROM=Monitoring App <noreply@example.com>
SMTP_SECURE=false

# --- Agent skanujący ---
MAX_CONCURRENCY=3
AGENT_LOOP_MS=60000
STATIC_TIMEOUT_MS=20000

# --- LLM/Ollama (opcjonalne; wymagane dla pipeline analizy zmian LLM) ---
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_TEXT_MODEL=qwen2.5:7b
LLM_MODEL=qwen2.5:7b
